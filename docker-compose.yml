services:

  gateway:
    build: ./gateway
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  mcp:
    build: ./mcp-server
    ports:
      - "9000:9000"
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - gateway
